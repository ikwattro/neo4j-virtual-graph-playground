# Force all queries through the multi-stage (Calcite) query engine.
# SSE rejects schema-qualified table names like "default"."people"; MSE handles them fine.
pinot.broker.enableMultiStagePipeline=true
pinot.multistage.engine.enabled=true
