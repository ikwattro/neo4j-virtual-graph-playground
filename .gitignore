# macOS
.DS_Store

# Editor
.idea/
.vscode/
*.swp

# JDBC drivers — download manually, do not commit binaries
config/lakegraph/jdbc/*.jar
config/postgres/jdbc/*.jar
config/oracle/jdbc/*.jar

# PyIceberg local catalog tracker — generated during warehouse creation, not needed at runtime
iceberg_catalog.db

# SSL certificates — generated locally, do not commit private keys
ssl/
