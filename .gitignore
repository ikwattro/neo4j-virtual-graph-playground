# Environment — copy .env.template to .env and edit locally
.env

# macOS
.DS_Store

# Editor
.idea/
.vscode/
*.swp

# DuckDB JDBC driver — too large to commit, download manually (see LakeGraph/IceGraph setup)
config/lakegraph/jdbc/*.jar

# Pinot JDBC fat jar and Maven build output — built locally via build-pinot-jdbc.sh
config/pinot/jdbc/*.jar
config/pinot/build/target/

# ClickHouse JDBC driver — shaded uber-jar, download manually (see ClickHouse README setup)
config/clickhouse/jdbc/*.jar

# PyIceberg local catalog tracker — generated during warehouse creation, not needed at runtime
iceberg_catalog.db

