# macOS
.DS_Store

# Editor
.idea/
.vscode/
*.swp

# DuckDB JDBC driver — too large to commit, download manually (see LakeGraph/IceGraph setup)
config/lakegraph/jdbc/*.jar

# PyIceberg local catalog tracker — generated during warehouse creation, not needed at runtime
iceberg_catalog.db

